import  styles from "../page.module.css"

export default function Sobre() {
    return (
        <div className={styles.page}>
            <h1 className={styles.pageTitle}>Sobre</h1>
        </div>
    );
}